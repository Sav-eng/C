#include<stdio.h>
#include<queue>
using namespace std;
void bfs(int x,int y)
{
    for(i=0;i<4;i++)
    {
        if(tab[x+move[i]][y+move[i]])
    }
}
main()
{
    scanf("%d%d",&n,&m);
    for(i=0;i<n;i++)
    {
        for(j=0;j<m;j++)
        {
            scanf("%d",&tab[i][j]);
        }
    }
}
