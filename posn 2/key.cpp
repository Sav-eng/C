#include<stdio.h>
main()
{
    char [150][5];
    scanf("%d%d",&n,&k)
    for(i=0;i<3;i++)
    {
        scanf("%s",&a[i]);
    }
    _max = max(strlen(a[0]),strlen(a[2]));
    for(i=0;i<_max;i++)
    {
        if()
    }
}
