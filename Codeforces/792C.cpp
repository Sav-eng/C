#include<bits/stdc++.h>
using namespace std;
int sum;
char tab[100001];
int f(int x,int y)
{
    if(x==n)return 0;
    else
    {
        mem[x][y] =
    }
}
main()
{
    int i,n;
    scanf("%s",tab);
    n = strlen(tab);
    for(i=0;i<n;i++)
    {
        sum += (tab[i]-'0');
    }
    printf("%d",f(0,0));
